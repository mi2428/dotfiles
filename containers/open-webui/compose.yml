services:
  open-webui:
    build: .
    ports:
      - "127.0.0.1:${OPEN_WEBUI_PORT:-8080}:8080"
    environment:
      ENABLE_OLLAMA_API: "false"
      ENABLE_PERSISTENT_CONFIG: "false"
      OPENAI_API_BASE_URLS: "https://api.ai.sakura.ad.jp/v1;http://cptr:8000/v1"
      OPENAI_API_KEYS: "${SAKURA_AI_ACCOUNT_TOKEN:?set SAKURA_AI_ACCOUNT_TOKEN};${CPTR_GATEWAY_API_KEY:-bootstrap}"
      OPENAI_API_CONFIGS: >-
        {"0":{"enable":true,"model_ids":["${SAKURA_AI_MODEL:-gpt-oss-120b}"]},"1":{"enable":true,"headers":{"X-OpenWebUI-Chat-Id":"{{CHAT_ID}}","X-OpenWebUI-Message-Id":"{{MESSAGE_ID}}","X-OpenWebUI-User-Message-Id":"{{USER_MESSAGE_ID}}","X-OpenWebUI-User-Message-Parent-Id":"{{USER_MESSAGE_PARENT_ID}}","X-OpenWebUI-Task":"{{TASK}}"}}}
      WEBUI_ADMIN_EMAIL: "${WEBUI_ADMIN_EMAIL:-admin@localhost}"
      WEBUI_ADMIN_NAME: "${CPTR_ADMIN_USERNAME:?set CPTR_ADMIN_USERNAME}"
      WEBUI_ADMIN_PASSWORD: "${CPTR_ADMIN_PASSWORD:?set CPTR_ADMIN_PASSWORD}"
      WEBUI_SECRET_KEY: "${WEBUI_SECRET_KEY:?set WEBUI_SECRET_KEY}"
    volumes:
      - open-webui-data:/app/backend/data
    depends_on:
      - cptr
    restart: unless-stopped

  cptr:
    image: ghcr.io/open-webui/computer:0.9.21-browser
    volumes:
      - cptr-data:/data
      - "${CPTR_WORKSPACE_DIR:?set CPTR_WORKSPACE_DIR}:/workspace"
    working_dir: /workspace
    restart: unless-stopped

volumes:
  open-webui-data:
  cptr-data:
