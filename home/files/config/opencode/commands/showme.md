---
description: Show the current topic as a rich visual
---

Use the `showme` skill to create a rich HTML visualization of the current
topic and open it beside this conversation with `terminal-browser`.

Focus on this optional request when supplied:

$ARGUMENTS

Treat blank arguments as a request to visualize the current discussion. Always
create and open the HTML artifact; do not substitute an inline-only diagram.
