#!/bin/bash
if [ $# != 0 ]; then
	for c in $*; do
		if [ -e $str ]; then
			str="$c"
		else
			str="$str+$c"
		fi
	done
	opt="index.php"
	opt="${opt}?search=${str}"
fi
dwb -n https://wiki.archlinuxjp.org/$opt > /dev/null 2>&1 &
