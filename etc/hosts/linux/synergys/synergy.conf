section: screens
    whipped-cream:
    matcha-latte:
    pad-ka-prao:
    yum-woon-sen:
end

section: links
    matcha-latte:
        left = whipped-cream
        # up = whipped-cream
    # pad-ka-prao:
    #     left = matcha-latte
    whipped-cream:
        right = matcha-latte
        # down = matcha-latte
end

# section: options
#     keystroke(alt+[) = switchInDirection(left)
#     keystroke(alt+]) = switchInDirection(right)
# end
